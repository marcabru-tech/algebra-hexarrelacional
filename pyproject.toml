[project]
name = "algebra-hexarrelacional"
version = "0.1.0"
description = "Álgebra Hexarrelacional de Significância para Algoritmos"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
